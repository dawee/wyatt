
var Ti = require('titanium-namespace');
var ViewElement = require('./view');

var ProgressbarElement = ViewElement.extend({

  create: function (options) {
    // TODO
  },

});

module.exports = ProgressbarElement;
