var Titanium = require('titanium-namespace');
var wyatt = require('../wyatt.mockti');

exports['test String#length'] = function(beforeExit, assert) {
    
    //wyatt.yat({'@type': 'View'});

    assert.equal('object', typeof Titanium.UI);
};